{
  "name": "agent-of-empires-website",
  "type": "module",
  "scripts": {
    "copy-assets": "cp ../assets/logo.svg ../assets/logo.png ../assets/logo-youtube.png ../assets/social-preview.png public/assets/ && cp ../docs/assets/*.gif ../docs/assets/*.png public/assets/ && mkdir -p public/assets/cockpit public/assets/web && cp ../docs/assets/cockpit/*.png public/assets/cockpit/ && cp ../docs/assets/web/*.png public/assets/web/",
    "sync-docs": "node scripts/sync-docs.mjs",
    "dev": "npm run copy-assets && npm run sync-docs && astro dev",
    "build": "npm run copy-assets && npm run sync-docs && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.2",
    "@tailwindcss/postcss": "^4.3.0",
    "astro": "^6.3.1",
    "tailwindcss": "^4.2.4"
  }
}
